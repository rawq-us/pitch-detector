{
  "name": "pitch-studio",
  "version": "1.45.0",
  "private": true,
  "description": "Single-file, in-browser music workstation — pitch detection, annotated memos, arps, beats, timeline. No build, no dependencies.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "start": "python3 -m http.server 8000"
  }
}
